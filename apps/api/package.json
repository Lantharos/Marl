{
  "name": "@sty/api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --ip 127.0.0.1 --port 42618 --inspector-port 42620",
    "check": "tsc --noEmit",
    "test": "bun test",
    "build": "wrangler deploy --dry-run --outdir dist"
  },
  "devDependencies": {
    "@sty/contracts": "workspace:*",
    "@types/bun": "^1.3.10",
    "typescript": "^6.0.2",
    "wrangler": "^4.85.0"
  },
  "dependencies": {
    "yaml": "^2.9.0"
  }
}
